// loop

var num = 0;

// while loop

while (num <= 10) {
  console.log(num);
  num++;
}

// for loop

var numbers = [22, 33, 44, 55, 66, 77, 88];

for (let i = 0; i <= numbers.length; i++) {
  var element = numbers[i];
  console.log(element);
}

// ### end code ###
